name: restapi-title-predicter

image:
  name: "radcom/restapi-title-predicter"
  tag: "1.0.0"
  pullPolicy: "Always"

replicaCount: 1

ingress:
  enabled: false
  hostname: restapi.title.predicter.minikube.com

service:
  port: 80

nodeSelector:
  enabled: false
  hostname: "machine-hostname"

config:
  listen: {}
